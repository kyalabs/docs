---
title: "The Honesty Layer"
icon: "star"
description: "How a ShoppiPanel report stays bound to what actually happened: the structural mechanisms that make an overstated report unreachable rather than merely discouraged."
---

<Note>
  **For the skeptic.** Why a report can't overclaim: the structural mechanisms that bind every headline to its evidence.
</Note>

A ShoppiPanel report makes claims about how AI shopping agents treated your brand: what they noticed, compared, recommended, and passed over. The value of those claims is the trust that can be placed in them. A report that overstates is worse than no report: it puts a confident, wrong sentence in front of a decision.

The Honesty Layer is the part of the pipeline that keeps the words in a report from drifting away from what the evidence supports. No human performs a review pass at the end to catch overstatement. The report is **built** so that the dishonest version is unreachable.

Four mechanisms do this work. Each one is laid out below, followed by why the result is structural rather than editorial.

---

## 1 · The headline cannot disagree with the body's numbers

A subtle failure mode in any automated study: the headline and the body of the _same_ report quietly disagree. The headline asserts one distribution; the detailed trip-by-trip evidence supports another. Each looks fine in isolation; together they are a fabrication.

Before a report can ship, every counted claim in the headline is reconciled against the audited trip evidence. The auditor and the producer of the headline read from the **same** computed distribution. There is one definition of which trips count, shared by both sides, so the check can never be blind to a contradictory headline. If the numbers the headline asserts do not match the numbers the evidence supports, the report is **held**: it cannot be delivered as clean collateral until the divergence is resolved.

This hard hold is one of only two places in the entire delivery path where a report is stopped rather than shipped-with-disclosure. Hard holds are reserved for this case: a headline whose distribution contradicts the audited counts. Everything else that could be wrong with a study is disclosed, not held.

<Note>
  This guard was the response to a live case where a study reached a successful state and presented a clean-looking headline whose distribution the evidence did not support. The detection had already flagged the run. What was missing was _prevention_. The hold is that prevention.
</Note>

The layer separates two situations:

<CardGroup cols={2}>
  <Card title="A claim mismatch: stops the report">
    The headline asserts something the audited counts do not support. This hard-holds. It cannot be exported as clean collateral until resolved.
  </Card>

  <Card title="A limited-evidence study: ships, marked">
    A study that simply came back with thin evidence is a different situation. It ships, clearly marked as not research-grade, never silently upgraded into something it isn't.
  </Card>
</CardGroup>

The system never launders a limited study into a confident one, and it never holds a study merely for being modest. Those are different defects, and the layer treats them differently.

---

## 2 · The headline is computed, then narrated, not the other way around

The single most consequential sentence in any report is the headline: the one-line verdict. The obvious risk is that a language model, asked to summarize a study, produces a fluent sentence the underlying trips do not actually support.

We removed that risk by changing the order of operations. Before any narrative is written, code computes the report's thesis directly from the audited evidence:

- which brand each agent landed on, and the tally across all trips;
- the canonical count of how many trips were informative versus planned;
- the verdict class: a clear lead, a contested lead, a genuine split, or no signal;
- a confidence band tied to how many trips actually carried evidence.

**The verdict is a computed object. The language model's job is to narrate that object; it cannot author a different one.** A draft headline that contradicts the computed verdict is replaced with the deterministic one before the report ships.

This inverts the usual failure mode. When a model proposes a conclusion and a checker tries to catch the bad ones, the checker is always one step behind. Computing the conclusion first turns that around: the thesis exists as a fact before a single sentence is drafted, and the narration is bound to it.

<Note>
  One concrete consequence: if a study has a dominant winner (say one brand led the majority of trips while four rivals each led one or two), the only headline the system can produce names that winner and states the distribution honestly ("leading, not unanimous"). A headline claiming "no single product won," when one clearly did, is not a sentence the pipeline can emit.
</Note>

The one field a model authors freely is the strategic implication, the "so what." Even that is bound to the evidence: it carries its supporting references, and on a validation failure it is omitted rather than fabricated. An absent sentence is preferable to an invented one.

---

## 3 · The claim boundary travels with the report

Every report carries an explicit statement of what it does and does not claim. A controlled-mission study of real AI shopping surfaces is:

- **not** a market-share measurement;
- **not** a forecast of human demand;
- **not** a public benchmark;
- **not** a ranking of model providers.

The claim boundary is a required field on the report's structure, set deterministically when the report is assembled, and rendered onto every delivered surface of the artifact. A report missing its claim boundary fails its own structural contract and does not ship.

The schema enforces the discipline: counts before rates, evidence references visible, the limits of the read stated on the face of the deliverable.

---

## 4 · A thin study ships marked; it is never dressed up

Agentic studies run against live, changing surfaces. Sometimes a study does not gather enough evidence to support a research-grade read. The dishonest move would be to dress up a thin study as a finished one. The honest move is to say so, in a typed, machine-readable way, and still hand you the artifact, clearly labelled for what it is.

A study that formed but did not clear the evidence bar **ships as a partial success, marked not research-grade**, not silently upgraded into something it isn't. That marking is a first-class, named state on the result (carried in the data, surfaced on the artifact), so a thin-but-usable read is visibly a thin read, never a confident one. The system does not hold a study merely for being modest, and it does not launder a modest study into a polished one. It ships it flagged.

A separate situation is a run that **could not form a study at all**. This is the terminal **failed** state, and it has two causes: the rig failed to operate, or too few of the panel's cells returned results to form a credible read. In either case the run collapses rather than shipping a hollow report; a study that cannot be assembled is not dressed up as one. This is distinct from the thin-but-usable study above, which formed and ships flagged. (Most of the panel's cells must return results for a study to form; the exact floor is qualitative.) The full buyer-facing state set (**Partial success**, **Failed**, and the held **Delayed** state that stays distinct from both) lives in [Run Lifecycle & Reliability](/evidence/run-lifecycle-and-reliability).

<Note>
  **A description of engine behavior.** The not-research-grade marking and the failed state describe how the pipeline behaves today: what the engine does with a thin study and with a run that cannot form one. Read this section as observed pipeline behavior, not as a promise about the outcome of any individual run.
</Note>

### A note on access

What an agent could and could not reach is described in two plain states, **reached** and **access-limited**, defined on [Reach](/insights/reading-a-report/reach); a finer characterization of _how_ a surface was access-limited rides on the per-finding receipt, underneath that headline state.

This access register is a **methodology discipline**, applied observe-only and graduating toward a stricter check over time. It is **not** a machine-guaranteed property of every sentence: it is how the language is disciplined, not a validator certifying every line today.

---

## Why this is structural, not editorial

All four mechanisms reduce to one sentence: **the dishonest output is made unreachable in code, rather than caught after the fact by review.**

A ShoppiPanel report rests on arithmetic, a schema contract, and a hold gate: all of which are auditable, and none of which depend on a human catching a mistake on the way out the door.

---

## Common questions

<AccordionGroup>
  <Accordion title="Do you rank models or claim market share?">
    No. Both are outside the claim boundary. A controlled-mission study reports observed agent behavior under stated conditions, with counts and evidence references. It is not a provider ranking, a market-share figure, or a demand forecast.
  </Accordion>
</AccordionGroup>